name: Run TAIRID Cosmology Penalized Model Comparison v3.3.1 Fresh

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tairid-cosmology-penalized-model-comparison-v3-3-1-fresh:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install numpy matplotlib

      - name: Confirm fresh script exists
        run: |
          pwd
          ls -la
          test -f run_tairid_cosmology_penalized_model_comparison_v3_3_1_fresh.py
          echo "v3.3.1 FRESH TAIRID cosmology penalized model comparison found."

      - name: Run TAIRID Cosmology Penalized Model Comparison v3.3.1 Fresh
        run: |
          python run_tairid_cosmology_penalized_model_comparison_v3_3_1_fresh.py

      - name: Upload fresh outputs
        uses: actions/upload-artifact@v4
        with:
          name: tairid-cosmology-penalized-model-comparison-v3-3-1-fresh-output
          path: |
            tairid_cosmology_penalized_model_comparison_v3_3_1_fresh_outputs/**
